From 2a0e4296c18bc39ee5588bf52f75fee4660c6e76 Mon Sep 17 00:00:00 2001
From: David Mohammed <fossfreedom@ubuntu.com>
Date: Sun, 8 Feb 2026 19:38:58 +0000
Subject: [PATCH] Rejig budgie-extras-daemon to just support layouts

---
 .../data/classicubuntubudgie.layout           | 51 +++++----------
 budgie-extras-daemon/data/cupertino.layout    | 22 ++-----
 budgie-extras-daemon/data/redmond.layout      |  6 +-
 budgie-extras-daemon/data/theone.layout       |  7 +--
 budgie-extras-daemon/data/ubuntubudgie.layout | 43 ++-----------
 budgie-extras-daemon/src/dbus.vala            | 15 +----
 .../src/examplebde/example.bde                | 14 -----
 budgie-extras-daemon/src/layouts.vala         | 63 ++++++++-----------
 budgie-extras-daemon/src/main.vala            | 10 +--
 budgie-extras-daemon/src/meson.build          |  6 --
 meson.build                                   |  2 +-
 11 files changed, 59 insertions(+), 180 deletions(-)
 delete mode 100644 budgie-extras-daemon/src/examplebde/example.bde

diff --git a/budgie-extras-daemon/data/classicubuntubudgie.layout b/budgie-extras-daemon/data/classicubuntubudgie.layout
index 00e53c9..8585c28 100644
--- a/budgie-extras-daemon/data/classicubuntubudgie.layout
+++ b/budgie-extras-daemon/data/classicubuntubudgie.layout
@@ -1,16 +1,15 @@
 [Panels]
-Panels=Top Panel;Bottom Panel
+Panels=Top Panel
 
 [Top Panel]
-Children=Budgie Menu;Spacer 1;ShowTime;Clock;AppIndicator;Tray;DropBy;QuickNote;Spacer 2;Night;Places;Notifications;Status;Separator;Spacer 3;User Indicator;Raven Trigger;
+Children=AppMenu;ShowTime;Clock;DropBy;AppIndicator;VisualSpace;QuickNote;Places;Notifications;Network;Status;Separator;User Indicator;Raven Trigger;
 Position=Top
+Size=37
 Transparency=dynamic
+Spacing=10
 
-[Budgie Menu]
-ID=Budgie Menu
-
-[Spacer 1]
-ID=Spacer
+[AppMenu]
+ID=AppMenu
 
 [ShowTime]
 ID=ShowTime
@@ -23,8 +22,8 @@ Alignment=center
 ID=AppIndicator Applet
 Alignment=end
 
-[Tray]
-ID=System Tray
+[VisualSpace]
+ID=Visual Space
 Alignment=end
 
 [DropBy]
@@ -35,14 +34,6 @@ Alignment=end
 ID=QuickNote
 Alignment=end
 
-[Spacer 2]
-ID=Spacer
-Alignment=end
-
-[Night]
-ID=Night Light
-Alignment=end
-
 [Places]
 ID=Places
 Alignment=end
@@ -51,36 +42,22 @@ Alignment=end
 ID=Notifications
 Alignment=end
 
-[Status]
-ID=Status Indicator
+[Network]
+ID=Network
 Alignment=end
 
-[User Indicator]
-ID=User Indicator
+[Status]
+ID=Status Indicator
 Alignment=end
 
 [Separator]
 ID=Separator
 Alignment=end
 
-[Spacer 3]
-ID=Spacer
+[User Indicator]
+ID=User Indicator
 Alignment=end
 
 [Raven Trigger]
 ID=Raven Trigger
 Alignment=end
-
-[Bottom Panel]
-Children=Icon Task List;
-Position=Bottom
-Size=50
-Transparency=always
-Autohide=intelligent
-Shadow=false
-Spacing=2
-Dock=true
-
-[Icon Task List]
-ID=Icon Task List
-Alignment=start
diff --git a/budgie-extras-daemon/data/cupertino.layout b/budgie-extras-daemon/data/cupertino.layout
index efcac77..409dfde 100644
--- a/budgie-extras-daemon/data/cupertino.layout
+++ b/budgie-extras-daemon/data/cupertino.layout
@@ -1,8 +1,8 @@
 [Panels]
-Panels=Top Panel;Bottom Panel
+Panels=Top Panel
 
 [Top Panel]
-Children=AppMenu;Spacer 1;Global Menu;AppIndicator;DropBy;Network;Spacer 2;Status;Clock;Notifications;;Raven Trigger;
+Children=AppMenu;Spacer 1;IconTaskList;AppIndicator;DropBy;Network;Spacer 2;Status;Clock;Notifications;;Raven Trigger;
 Position=Top
 Size=37
 Transparency=dynamic
@@ -13,8 +13,8 @@ ID=AppMenu
 [Spacer 1]
 ID=Spacer
 
-[Global Menu]
-ID=Global Menu
+[IconTaskList]
+ID=Icon Task List
 
 [Clock]
 ID=Clock
@@ -75,17 +75,3 @@ Alignment=end
 [Raven Trigger]
 ID=Raven Trigger
 Alignment=end
-
-[Bottom Panel]
-Children=Icon Task List;
-Position=Bottom
-Size=50
-Transparency=always
-Autohide=intelligent
-Shadow=false
-Spacing=2
-Dock=true
-
-[Icon Task List]
-ID=Icon Task List
-Alignment=start
diff --git a/budgie-extras-daemon/data/redmond.layout b/budgie-extras-daemon/data/redmond.layout
index 698c500..6d72405 100644
--- a/budgie-extras-daemon/data/redmond.layout
+++ b/budgie-extras-daemon/data/redmond.layout
@@ -2,7 +2,7 @@
 Panels=Bottom Panel
 
 [Bottom Panel]
-Children=AppMenu;Spacer 1;IconTaskList;AppIndicator;DropBy;Notifications;Status;Spacer 3;Network;Clock;Raven Trigger;
+Children=AppMenu;Spacer 1;TaskList;AppIndicator;DropBy;Notifications;Status;Spacer 3;Network;Clock;Raven Trigger;
 Position=Bottom
 Size=37
 Transparency=dynamic
@@ -13,8 +13,8 @@ ID=AppMenu
 [Spacer 1]
 ID=Spacer
 
-[IconTaskList]
-ID=Icon Task List
+[TaskList]
+ID=Task List
 Alignment=start
 
 [Clock]
diff --git a/budgie-extras-daemon/data/theone.layout b/budgie-extras-daemon/data/theone.layout
index 1d2f670..4fb6295 100644
--- a/budgie-extras-daemon/data/theone.layout
+++ b/budgie-extras-daemon/data/theone.layout
@@ -2,7 +2,7 @@
 Panels=Top Panel;Side Panel;
 
 [Top Panel]
-Children=Spacer 1;GlobalMenu;AppIndicator;DropBy;Network;Notifications;Status;Clock; User Indicator;Raven Trigger;
+Children=Spacer 1;AppIndicator;DropBy;Network;Notifications;Status;Clock; User Indicator;Raven Trigger;
 Position=Top
 Size=37
 Transparency=dynamic
@@ -13,9 +13,6 @@ ID=AppMenu
 [Spacer 1]
 ID=Spacer
 
-[GlobalMenu]
-ID=Global Menu
-
 [Clock]
 ID=Clock
 Alignment=end
@@ -48,8 +45,6 @@ Alignment=end
 ID=Raven Trigger
 Alignment=end
 
-
-
 [Side Panel]
 Children=Spacer 2;AppMenu;Spacer 3;Places;Recent;IconTaskList;Trash;Spacer 3;
 Position=Left
diff --git a/budgie-extras-daemon/data/ubuntubudgie.layout b/budgie-extras-daemon/data/ubuntubudgie.layout
index f2adc20..52e9610 100644
--- a/budgie-extras-daemon/data/ubuntubudgie.layout
+++ b/budgie-extras-daemon/data/ubuntubudgie.layout
@@ -1,35 +1,20 @@
 [Panels]
-Panels=Top Panel;Bottom Panel
+Panels=Top Panel
 
 [Top Panel]
-Children=AppMenu;ShowTime;Clock;DropBy;AppIndicator;VisualSpace;QuickNote;Places;Notifications;Network;Status;Separator;User Indicator;Raven Trigger;
+Children=AppMenu;AppIndicator;QuickNote;Places;Notifications;Network;Status;Clock;Raven Trigger;
 Position=Top
 Size=37
-Transparency=dynamic
+Transparency=none
 Spacing=10
 
 [AppMenu]
 ID=AppMenu
 
-[ShowTime]
-ID=ShowTime
-
-[Clock]
-ID=Clock
-Alignment=center
-
 [AppIndicator]
 ID=AppIndicator Applet
 Alignment=end
 
-[VisualSpace]
-ID=Visual Space
-Alignment=end
-
-[DropBy]
-ID=DropBy
-Alignment=end
-
 [QuickNote]
 ID=QuickNote
 Alignment=end
@@ -50,28 +35,10 @@ Alignment=end
 ID=Status Indicator
 Alignment=end
 
-[Separator]
-ID=Separator
-Alignment=end
-
-[User Indicator]
-ID=User Indicator
+[Clock]
+ID=Clock
 Alignment=end
 
 [Raven Trigger]
 ID=Raven Trigger
 Alignment=end
-
-[Bottom Panel]
-Children=Icon Task List;
-Position=Bottom
-Size=50
-Transparency=always
-Autohide=intelligent
-Shadow=false
-Spacing=2
-Dock=true
-
-[Icon Task List]
-ID=Icon Task List
-Alignment=start
diff --git a/budgie-extras-daemon/src/dbus.vala b/budgie-extras-daemon/src/dbus.vala
index 06f8e88..18bd1d4 100644
--- a/budgie-extras-daemon/src/dbus.vala
+++ b/budgie-extras-daemon/src/dbus.vala
@@ -35,13 +35,11 @@ public const string EXTRAS_DBUS_OBJECT_PATH = "/org/ubuntubudgie/extrasdaemon";
 [DBus (name = "org.UbuntuBudgie.ExtrasDaemon")]
 public class DbusManager : Object
 {
-    KeybinderManager keybinder;
     Layouts.LayoutsManager layoutmanager;
 
     [DBus (visible = false)]
-    public DbusManager(KeybinderManager km)
+    public DbusManager()
     {
-        keybinder = km;
         layoutmanager = new Layouts.LayoutsManager();
     }
 
@@ -73,22 +71,13 @@ public class DbusManager : Object
      */
 
      public bool ReloadShortcuts() throws Error {
-        return keybinder.reload();
+        return true;
      }
 
     /**
      * Find the shortcut key string for the bde file name key
      */
     public string GetShortcut(string key_name) throws Error {
-        string shortcut = "";
-
-        var ret = keybinder.get_shortcut(key_name, out shortcut);
-
-        if (ret)
-        {
-            debug("output %s", shortcut);
-            return shortcut;
-        }
 
         debug("nothing to return");
         return "";
diff --git a/budgie-extras-daemon/src/examplebde/example.bde b/budgie-extras-daemon/src/examplebde/example.bde
deleted file mode 100644
index 62b6723..0000000
--- a/budgie-extras-daemon/src/examplebde/example.bde
+++ /dev/null
@@ -1,14 +0,0 @@
-# Example file for services that require global keybinding
-# services drop a .bde extension file into
-# /etc/budgie-extras-daemon/bde/ or the install location appended with bde
-# either/both toggle / command need to be specified
-#
-# [Daemon]
-# shortcut=key shortcut to bind e.g. <ctrl><alt>s
-# toggle=GSettings full-gsettings-path key-is-bool-to-toggle-when-shortcut-activated
-# command=command line to execute when shortcut activated
-# onlyactivate=GSettings full-gsettings-path key-is-bool-bind-shortcut-only-when-true
-# overlay=GSettings full-gsettings-path key-is-string-set-to-empty
-#   used with onlyactivate to set key to '' if true or reset to default key
-#   value if onlyactivate is false
-# name=short name to describe what this shortcut is all about.
diff --git a/budgie-extras-daemon/src/layouts.vala b/budgie-extras-daemon/src/layouts.vala
index 2cd5af8..9e25c08 100644
--- a/budgie-extras-daemon/src/layouts.vala
+++ b/budgie-extras-daemon/src/layouts.vala
@@ -15,12 +15,8 @@
 */
 
 namespace Layouts {
-
-    const string plank_schema="net.launchpad.plank";
-    const string plank_settings_schema="net.launchpad.plank.dock.settings";
-    const string plank_path="/net/launchpad/plank/docks/xyz/";
     const string panel_schema="com.solus-project.budgie-panel";
-    const string plank_global_path="/usr/share/applications/plank.desktop";
+    const string crystal_dock_global_path="/usr/share/applications/crystal-dock.desktop";
     const string appmenu_budgie_schema="org.ubuntubudgie.plugins.budgie-appmenu";
     const string budgiewm_schema="com.solus-project.budgie-wm";
     const string nemo_window_schema="org.nemo.window-state";
@@ -43,11 +39,11 @@ namespace Layouts {
 
         }
 
-        private void stop_plank () {
-            run_cmd("killall plank");
+        private void stop_crystal_dock () {
+            run_cmd("killall crystal-dock");
 
             string autostart_file = Environment.get_home_dir() +
-                "/.config/autostart/plank.desktop";
+                "/.config/autostart/crystal-dock.desktop";
 
             if (! FileUtils.test(autostart_file, FileTest.EXISTS)) {
                 debug("does not exist %s", autostart_file);
@@ -78,32 +74,14 @@ namespace Layouts {
             });
         }
 
-        private void start_plank(bool centered=false) {
-            stop_plank();
+        private void start_crystal_dock(bool centered=false) {
+            stop_crystal_dock();
 
-            if (! FileUtils.test(plank_global_path, FileTest.EXISTS)) {
-                debug("does not exist %s", plank_global_path);
+            if (! FileUtils.test(crystal_dock_global_path, FileTest.EXISTS)) {
+                debug("does not exist %s", crystal_dock_global_path);
                 return;
             }
 
-            if (centered) {
-                var plank_settings = new GLib.Settings(plank_schema);
-                var docks = plank_settings.get_strv("enabled-docks");
-
-                foreach (string dock in docks) {
-                    var path = plank_path.replace("xyz", dock);
-                    var settings = new GLib.Settings.with_path(plank_settings_schema, path);
-
-                    settings.set_string("position", "bottom");
-                    settings.set_string("theme", "Transparent");
-                    settings.set_int("icon-size", 32);
-                    settings.set_int("hide-delay", 500);
-                    settings.set_string("hide-mode", "window-dodge");
-                    settings.set_boolean("zoom-enabled", true);
-                    break; // we assume the first dock is the primary
-                }
-            }
-
             try {
                 string autostart_folder = Environment.get_home_dir() +
                     "/.config/autostart/";
@@ -113,15 +91,15 @@ namespace Layouts {
                     folder.make_directory();
                 }
 
-                File file = File.new_for_path(plank_global_path);
-                File dest = File.new_for_path(autostart_folder + "plank.desktop");
+                File file = File.new_for_path(crystal_dock_global_path);
+                File dest = File.new_for_path(autostart_folder + "crystal-dock.desktop");
                 file.copy(dest, FileCopyFlags.OVERWRITE);
             }
             catch (Error e) {
                 warning("Cannot copy: %s", e.message);
             }
 
-            run_cmd("nohup plank &>/dev/null", true);
+            run_cmd("nohup crystal-dock &>/dev/null", true);
         }
 
         private void appmenu_powerstrip(bool enable) {
@@ -132,6 +110,14 @@ namespace Layouts {
             settings.set_boolean("enable-powerstrip", enable);
         }
 
+        private void appmenu_sidebar(bool enable) {
+            var schema = GLib.SettingsSchemaSource.get_default ().lookup (appmenu_budgie_schema, true);
+            if (schema == null)
+                return;
+            var settings = new GLib.Settings(appmenu_budgie_schema);
+            settings.set_boolean("enable-favorites", enable);
+        }
+
         private void appmenu_categoryview(bool show_category) {
             var schema = GLib.SettingsSchemaSource.get_default ().lookup (appmenu_budgie_schema, true);
             if (schema == null)
@@ -151,27 +137,30 @@ namespace Layouts {
         }
 
         private void reset_panel() {
-            run_cmd ("nohup budgie-panel --reset --replace &>/dev/null", true);
+            run_cmd ("nohup budgie-panel --reset --reset-raven --replace &>/dev/null", true);
         }
 
         public void reset(string layout_name) {
 
-            stop_plank();
+            stop_crystal_dock();
             appmenu_powerstrip(false);
             appmenu_categoryview(false);
+            appmenu_sidebar(false);
             leftside_buttons(false);
             show_nemo_menu(false);
 
             switch (layout_name) {
                 case "ubuntubudgie": {
-                    // no customisations needed
+                    start_crystal_dock(true);
+                    appmenu_sidebar(true);
                     break;
                 }
                 case "classicubuntubudgie": {
-                    // no customisations needed
+                    start_crystal_dock(true);
                     break;
                 }
                 case "cupertino": {
+                    start_crystal_dock(true);
                     appmenu_powerstrip(true);
                     appmenu_categoryview(true);
                     leftside_buttons();
diff --git a/budgie-extras-daemon/src/main.vala b/budgie-extras-daemon/src/main.vala
index b740716..6dd5742 100644
--- a/budgie-extras-daemon/src/main.vala
+++ b/budgie-extras-daemon/src/main.vala
@@ -52,8 +52,6 @@ public static int main(string[] args) {
     Gtk.init(ref args);
     OptionContext ctx;
 
-    BudgieExtras.KeybinderManager? manager = null;
-
     ctx = new OptionContext("- Budgie Extras Daemon");
     ctx.set_help_enabled(true);
     ctx.add_main_entries(options, null);
@@ -66,13 +64,11 @@ public static int main(string[] args) {
         return 0;
     }
 
-    manager = new BudgieExtras.KeybinderManager(replace);
-
+    BudgieExtras.DbusManager dbus = new BudgieExtras.DbusManager();
+    dbus.setup_dbus(replace);
+    
     /* Enter main loop */
     Gtk.main();
 
-    /* Deref - clean */
-    manager = null;
-
     return 0;
 }
diff --git a/budgie-extras-daemon/src/meson.build b/budgie-extras-daemon/src/meson.build
index 2549106..5dc8121 100644
--- a/budgie-extras-daemon/src/meson.build
+++ b/budgie-extras-daemon/src/meson.build
@@ -13,7 +13,6 @@ subdir('config')
 
 ExtrasDaemonSources = [
 'main.vala',
-'manager.vala',
 'dbus.vala',
 'layouts.vala'
 ]
@@ -46,11 +45,6 @@ executable(
     install_dir: join_paths(prefix, get_option('bindir'))
 )
 
-install_data(
-    join_paths('.', 'examplebde', 'example.bde'),
-    install_dir: join_paths(prefix, get_option('datadir'), daemon_name)
-)
-
 install_data('invoke.py',
     install_dir: LIB_INSTALL_DIR)
 
diff --git a/meson.build b/meson.build
index 570a3cb..92dfdd4 100644
--- a/meson.build
+++ b/meson.build
@@ -215,7 +215,7 @@ if build_recommended == true or build_takeabreak == true
 endif
 
 build_extrasdaemon = get_option('build-extrasdaemon')
-if for_wayland == false and (build_recommended == true or build_extrasdaemon == true)
+if build_recommended == true or build_extrasdaemon == true
 	subdir('budgie-extras-daemon')
 endif
 
-- 
2.51.0

